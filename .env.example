# Server-side only. Without a key the app runs in mock mode (scripted responses).
ANTHROPIC_API_KEY=
# Optional overrides
MAIN_MODEL=claude-opus-5
# Set to "off" to disable server-side refusal fallbacks on the main agent
MAIN_FALLBACKS=
# Force mock mode even when a key is set
MOCK_MAIN=
